
  $(function(){
	  $("#home_type_effect").typed({
		strings: ["we are more than just gamers"], 
		contentType: 'html',
		showCursor: false,
		// time before typing starts
		startDelay: 1200,
		// backspacing speed
		backSpeed: 30,
		cursorChar: "_",
		typeSpeed: 40
	  });
  });
